/**
 *
 *  尝试完成如下功能：
 *
 *  const array = [1, 2, 3];
 *
 *  filter(array, function(item){
 *      return item % 2 === 0;
 *  }); // [2]
 *
 *
 **/
const filter = function(arr, iteratee) {
    //在这里实现
};

module.exports = filter;
