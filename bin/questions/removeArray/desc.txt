## 题目要求：

* 在当前路径的`removeArray`下，找到`index.js`，并补全实现代码
* 再次执行`fe`，选择该测试题目，然后选择"检验答题结果"
* 检验自己的测试结果

## 提示：
### 你的`removeArray`模块应该完成如下功能：

```javascript
var removeArray = require('removeArray');

var arr = [1, 2, 3, 4];
removeArray(arr, 0);
console.log(arr); // [2, 3, 4]

removeArray(arr, 2);
console.log(arr); // [2, 3]

removeArray(arr, 3);
console.log(arr); // [2, 3]
```
