'use strict';

/**
 *
 *  尝试完成如下功能：
 *
 *  var arr1 = ['a', 'b'];
 *
 *  flatmap(arr1, function(item){
 *      return [item, item];
 *  }); // ['a', 'a', 'b', 'b']
 *
 *
 **/
var flatmap = function(array, iteratee) {
    //在这里实现
};

module.exports = flatmap;
