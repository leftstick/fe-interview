'use strict';

/**
 *
 *  该模块必须完成以下功能：
 *  为简化题目，currying模块仅接受只有两个参数的函数
 *  
 *  var currying = require('currying');
 *
 *  var add = function(a, b) {
 *     return a + b;
 *  };
 *
 *  var curried = curry(add);
 *  console.log(curried(1)(2)); // 3
 *
 **/
var currying = function(func) {

};

module.exports = currying;
